/*9. Write a C function that takes two integers as parameters and returns the value of the 
larger one. */
#include <stdio.h>
#include <math.h>

int main(){
      int Number_1 , Number_2;
      printf("Please enter the first Number :\n ");
      scanf("%d",&Number );
      peerintf("Please enter the second Number :\n ");
      scanf ("%d", &Number_2 );

      printf("The larger number is : %d \n" , larger(Number_1 , Number_2));

      return 0 ;
}