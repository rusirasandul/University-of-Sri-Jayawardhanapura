#include <stdio.h>
int main()
{
    int k = 5; // Example initialization of k
    int i = k + 2;
    printf("The number i is %d\n", i); // Using %d to print the integer value of i
    
    int a, b, c;
    a = b = c = 2; // Correctly initializes i, j, and k to 2
    printf("The numbers a, b, and c are %d %d %d\n", a, b, c);
    return 0;

}
